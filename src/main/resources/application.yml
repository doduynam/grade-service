
logging:
  level:
    org:
      hibernate:
        type: trace
        SQL: debug

spring:
  graphql:
    graphiql:
      enabled: true

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://0.0.0.0:4000/state_management
    username: admin
    password: admin

warrior:
  nodejs-api-url: http://localhost:8090

